{
  "name": "agent-browser-testid",
  "version": "1.0.0",
  "description": "Render a URL and print a compact DOM tree with data-testid attributes",
  "bin": {
    "dom-testid": "bin/dom-testid.js"
  },
  "scripts": {
    "lint": "eslint . && prettier --check .",
    "lint:fix": "eslint . --fix && prettier --write .",
    "test": "node --test",
    "postinstall": "playwright install chromium"
  },
  "keywords": [
    "cli",
    "dom",
    "playwright",
    "testid"
  ],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "playwright": "^1.54.2"
  },
  "devDependencies": {
    "@eslint/js": "^9.33.0",
    "eslint": "^9.33.0",
    "eslint-config-prettier": "^10.1.8",
    "globals": "^16.3.0",
    "prettier": "^3.6.2"
  }
}
